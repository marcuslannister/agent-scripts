interface:
  display_name: "Maintainer Orchestrator"
  short_description: "Coordinate Codex app project workers"
  default_prompt: "Use $maintainer-orchestrator to coordinate repository execution in dedicated Codex app threads, keep collaboration subagents read-only and support-only, protect forgotten work, serialize exact-head landing, capture durable decisions, audit dependencies, and propose verified releases."
